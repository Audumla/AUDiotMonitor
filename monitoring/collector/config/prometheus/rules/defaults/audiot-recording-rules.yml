groups:
  - name: audiot_gpu_vram_calculations
    interval: 15s
    rules:
      - record: audiot_gpu_vram_used_bytes
        expr: |
          sum by (host, device_id) (
            hw_device_capacity_bytes{device_class="gpu", component="memory", sensor="usage"}
          )

      - record: audiot_gpu_vram_capacity_bytes
        expr: |
          sum by (host, device_id) (
            hw_device_capacity_bytes{device_class="gpu", component="memory", sensor="capacity"}
          )

      - record: audiot_gpu_vram_usage_percent
        expr: |
          (
            audiot_gpu_vram_used_bytes / audiot_gpu_vram_capacity_bytes
          ) * 100

